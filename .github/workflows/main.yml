name: Keep Space Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Hugging Face Space
        run: |
          for i in {1..10}
          do
            curl -s https://0xhexz-hex.hf.space > /dev/null
            echo "Ping $i at $(date) - Status: Success"
            sleep 30
          done
